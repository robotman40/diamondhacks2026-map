// Supabase client — fill in your project URL and anon key when ready.
// Get these from: https://supabase.com/dashboard → Project Settings → API
//
// To activate:
//   1. npm install @supabase/supabase-js
//   2. Add to .env.local:
//        EXPO_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
//        EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
//   3. Uncomment the block below and delete the stub export.

/*
import { createClient } from "@supabase/supabase-js";

const supabaseUrl = process.env.EXPO_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
*/

// Stub — replace with the real client above once credentials are ready.
export const supabase = null as null;
